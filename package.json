{
  "name": "hvac-ai",
  "version": "0.1.0",
  "description": "AI front-office for HVAC contractors — safety gate, conversational intake, FSM integration",
  "type": "module",
  "scripts": {
    "start":  "node src/server.js",
    "dev":    "node --watch src/",
    "test":   "node --test tests/*.test.js",
    "safety-gate": "node src/lib/safety-gate.js",
    "lint":   "eslint src/"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0",
    "dotenv": "^16.6.1",
    "express": "^4.22.2",
    "twilio": "^5.13.1"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
